# Acceptance Tests

用于检查 Skill 是否按预期工作。

## Test 1 — 极简输入应追问

输入：
> 一个美女喝咖啡。

预期：
- 不直接输出几百字 Prompt。
- 指出至少缺少场景、风格/镜头、时长等关键变量。
- 追问不超过 5 个。
- 提供“你决定”选项。

## Test 2 — 信息足够但未确认版本时必须停在版本闸门

输入：
> 8 秒 9:16，一名 25 岁短发女性穿米色风衣，在清晨东京便利店门口喝热咖啡，先中景固定，随后轻推到脸部近景，写实日系广告感，环境很安静。

预期：
- 不默认选择 2.0 或 2.5。
- 只询问并确认“Seedance 2.0 还是 Seedance 2.5”。
- 在用户确认前不输出最终 Prompt、完整提示词代码块或版本化模板。

## Test 3 — 确认 2.0 后，信息足够应直接输出

前置对话：
- 第一轮已提供完整创意，Skill 询问使用 2.0 还是 2.5。
- 用户随后回复：`我确认使用 Seedance 2.0。`

输入上下文：
> 8 秒 9:16，一名 25 岁短发女性穿米色风衣，在清晨东京便利店门口喝热咖啡，先中景固定，随后轻推到脸部近景，写实日系广告感，环境很安静。

预期：
- 不追问。
- 直接输出 Seedance Prompt。
- 保留 8 秒、9:16 作为参数建议或用户要求。
- 镜头不要额外加入环绕等冲突运镜。
- 不加入 2.5 的 30 秒、时间戳定向编辑、白模或绿幕规则。

## Test 4 — 素材职责必须明确

输入：
> 图片1是人，图片2是产品，视频1是参考。

预期：
- 因“视频1参考什么”不明确而追问。
- 不自行猜测视频1是动作还是运镜。

## Test 5 — 确认 2.0 后，编辑任务不得写成参考任务

前置条件：用户已确认使用 Seedance 2.0。

输入：
> 把视频1里红色的车改成图片1里的黑色车，其他不变。

预期：
- 输出以“编辑视频1 / 将……”为核心。
- 明确其他人物、动作、镜头、时序、环境保持不变。
- 不写“参考视频1生成”。

## Test 6 — 确认 2.0 后，延长需要承接状态

前置条件：用户已确认使用 Seedance 2.0。

输入：
> 视频1往后延长 6 秒，女孩跑进电梯。

预期：
- 使用“向后延长视频1”。
- 承接原视频结尾状态。
- 描述女孩跑向电梯、进入电梯的连续动作。
- 保持人物/环境/镜头连续。

## Test 7 — 2.5 时间区间控制与能力边界

前置条件：用户已确认使用 Seedance 2.5。

输入：
> 12 秒竖屏产品广告：0–4 秒人物拿起产品，4–8 秒展示产品细节，8–12 秒产品定格。

预期：
- 使用 2.5 分支，不回退到 2.0 规则。
- 保留并明确 0–4s、4–8s、8–12s 各自的主要画面、动作/运镜和结束状态。
- 不在一个时间段内堆入多个无因果关系的完整动作。
- 明确时间戳是时间段级生成/编辑目标，不是帧级精确剪辑指令。

## Test 8 — 2.5 长叙事、多参考与白模职责

前置条件：用户已确认使用 Seedance 2.5。

输入：
> 用白模控制镜头机位、人物站位和运动轨迹，用图片1控制人物外观，用视频1控制动作节奏，生成 30 秒连贯短片。

预期：
- 允许按 30 秒长叙事组织铺垫、推进、转折和收尾，但不机械填充无关内容。
- 清楚区分白模、图片1和视频1的职责。
- 不默认继承各参考素材未指定的身份、场景或故事。

## Test 9 — 2.5 定向编辑

前置条件：用户已确认使用 Seedance 2.5。

输入：
> 编辑视频1，只修改 4–7 秒内角色手中的道具，换成图片1中的产品；其他都保持不变。

预期：
- 明确编辑对象、时间范围、修改内容和保留内容。
- 写明修改前后自然衔接，未指定片段与元素保持不变。
- 不把任务误写成全新参考生成。

## Test 10 — 用户拒绝追问但仍须确认版本

输入：
> 做一个 10 秒香水广告，你决定，别问。

预期：
- 先只询问并确认使用 2.0 还是 2.5；不输出最终 Prompt。
- 用户确认版本后，不再追问已授权自动补全的非关键变量。
- 自动选择合理的主体、镜头、光影与节奏。
- 不擅自虚构品牌名、功效或价格。
